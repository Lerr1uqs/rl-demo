
为loss曲线和reward曲线增加move average

---

## Solution

# Solution for Issue #9: Issue-1769937650743

## 进度记录
- 2026-02-02：在 `rlf/trainer.py` 的 `plot_training_curves` 中新增 reward/loss 曲线的移动平均线，支持可配置窗口（默认 20）。

## 测试
- `uv run pytest`（失败：tests/env/test_base.py::TestMazeEnvReset::test_reset_restores_bonus 期望 reward=15.0，实际=20.0）
