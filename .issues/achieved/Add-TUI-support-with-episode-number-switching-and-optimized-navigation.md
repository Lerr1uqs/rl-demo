
添加对tui的支持现在的需求有两个：

1. 能够自由切换 episode 的数字。比如按一个 e 键 弹出一个输入框，然后再输入数字，就可以直接切换到对应的 episode
2. 优化目前的切换逻辑：
   (a) 现在是从 1 切到 600，通过上下键操作非常慢，因为它必须按顺序从 1 到 2 再到 3，直到 606
   (b) 希望实现类似时钟的顺时针和逆时针逻辑，比如从 1 往上按可以直接变成 600，这个也要加上

---

## Solution

# Solution for Issue #7: Add-TUI-support-with-episode-number-switching-and-optimized-navigation

## 进度记录
- 2026-01-31：完成TUI的Episode数字输入缓冲、无效范围提示、回车/ESC清空逻辑；上下键支持首尾环绕切换；状态栏增加输入提示与快捷键说明。修改文件：`rlf/tui.py`。
- 2026-01-31：改为按 `e` 弹出浮动输入框，输入数字后回车跳转；增加输入框错误提示与ESC关闭逻辑；状态栏提示更新。修改文件：`rlf/tui.py`。
- 2026-01-31：修复 Textual CSS 不支持 `inset` 的报错，改为 `width/height: 100%` 覆盖全屏。修改文件：`rlf/tui.py`。
- 2026-01-31：修复按 `e` 无法弹窗的问题：隐藏时禁用输入框防止焦点抢占，并在显示/关闭时切换焦点。修改文件：`rlf/tui.py`。
- 2026-01-31：改为使用 `ModalScreen` 浮窗（不覆盖主界面），按 `e` 触发弹窗并回车跳转。修改文件：`rlf/tui.py`。
