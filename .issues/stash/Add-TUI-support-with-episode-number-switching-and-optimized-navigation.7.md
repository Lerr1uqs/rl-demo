---
Create Date: 2026-01-31
Type: feat
---

添加对tui的支持现在的需求有两个：

1. 能够自由切换 episode 的数字。比如按一个 1 键，然后再输入数字，就可以直接切换到对应的 episode
2. 优化目前的切换逻辑：
   (a) 现在是从 1 切到 600，通过上下键操作非常慢，因为它必须按顺序从 1 到 2 再到 3，直到 60
   (b) 希望实现类似时钟的顺时针和逆时针逻辑，比如从 1 往上按可以直接变成 600，这个也要加上